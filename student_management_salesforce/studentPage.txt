<apex:page controller="StudentController">
    <apex:form >
        <h1>Student Management Plateform</h1>
        <apex:pageBlock title="Add Student">
              <apex:pageBlockSection columns="1">
                  <apex:inputField value="{!stu.Name}" />
                  <apex:inputField value="{!stu.Roll_No__c}" />
                  <apex:inputField value="{!stu.Class__c}" />
                  <apex:inputField value="{!stu.Mobile_No__c}" />
              </apex:pageBlockSection>
              
              <apex:commandButton value="Save Student" action="{!addStudent}" rerender="studentTable" />
        </apex:pageBlock>

        <apex:pageBlock title="Student Records" id="studentTable">
            <apex:pageBlockTable value="{!studentList}" var="s">
                <apex:column value="{!s.Name}" />
                <apex:column value="{!s.Roll_No__c}" />
                <apex:column value="{!s.Class__c}" />
                <apex:column value="{!s.Mobile_No__c}" />
                <apex:column >
                    <apex:commandLink value="Delete" action="{!deleteStudent}">
                        <apex:param name="id" value="{!s.Id}" />
                    </apex:commandLink>
                </apex:column>
            </apex:pageBlockTable>
        </apex:pageBlock>
    </apex:form>
</apex:page>
           




public class StudentController {
    public Student__c stu {get; set;}
    public List<Student__c> studentList {get; set;}
    
    public StudentController(){
        stu = new Student__c();
        loadStudent();
    }
    
    public void addStudent(){
    insert stu;
        stu = new Student__c();
        loadStudent();
    }
    
    public void loadStudent(){
        studentList = [SELECT Id, Roll_No__c, Name, Class__c, Mobile_No__c FROM Student__c ];
    }
    
    public void deleteStudent(){
        String studentId = ApexPages.currentPage().getParameters().get('Id');
        Student__c s = [Select Id FROM Student__c WHERE Id =:studentId LIMIT 1];
        delete s;
        loadStudent();
    }
}